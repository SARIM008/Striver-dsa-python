def lsum(arr):
    m_sum=0
    r_sum=0
    for i in range(len(arr)):
        r_sum+=arr[i]
        if r_sum<0:
            r_sum=0
        elif m_sum <= r_sum:
            m_sum=r_sum
    return m_sum

arr= [2, 3, 5, -2, 7, -4]  
print(lsum(arr))